name: GitHub Actions Demo
run-name: ${{ github.actor }} is testing out GitHub Actions 🚀
on: [push]
jobs:
  Explore-GitHub-Actions:
    runs-on: ubuntu-latest
    steps:
      - run: echo "🎉 The job was automatically triggered by a ${{ github.event_name }} event."
      - run: echo "🐧 This job is now running on a ${{ runner.os }} server hosted by GitHub!"
      - run: echo "🔎 The name of your branch is ${{ github.ref }} and your repository is ${{ github.repository }}."
      - name: Check out repository code
        uses: actions/checkout@v4
      - run: echo "💡 The ${{ github.repository }} repository has been cloned to the runner."
      - run: echo "🖥️ The workflow is now ready to test your code on the runner."
      - name: List files in the repository
        run: |
          ls ${{ github.workspace }}
      - run: echo "🍏 This job's status is ${{ job.status }}."

  Simulation:
    runs-on: ubuntu-latest
    #container: ghdl/ghdl:ubuntu18-llvm-5.0
    steps:
      - name: '🧰 Checkout'
        uses: actions/checkout@v4
        with:
          submodules: true

      - name: 'Build Docker image with GHDL + Python'
        run: >-
          cd $GITHUB_WORKSPACE/ &&
          docker build -t ghdl_image .

      - name: 'Run HDLregression simulation with GHDL'
        run: >-
          docker run -v $GITHUB_WORKSPACE/:/work/axistream_uart/
          -w /work/axistream_uart/
          ghdl_image
          bash -c "bash ./docker_run_sim.sh"

      # - name: Log the step container node version
      #   uses: docker://ghdl/ghdl:ubuntu18-llvm-5.0
      #   with:
      #     entrypoint: usr/local/bin/echo
      #     args: -v

      # - name: Setup GHDL
      #   uses: ghdl/setup-ghdl-ci@nightly
      #   with:
      #     backend: gcc

      #- name: Print GHDL version
      #  run: ghdl --version

      #- name: Run HDLregression simulation
      #  run: |
      #    cd $GITHUB_WORKSPACE/
      #    mkdir run
      #    cd run
      #    python ../scripts/run.py -s ghdl
